{
  "name": "musicplayer",
  "version": "0.0.1",
  "main": "dist/index.js",
  "module": "dist/module.js",
  "type": "module",
  "scripts": {
    "dev": "npm run rebuild && node dist/index.js",
    "build": "npx parcel build src/index.ts",
    "clean": "rm -rf dist .parcel-cache",
    "rebuild": "npm run clean && npm run build"
  },
  "dependencies": {
    "thneed-gfx": "0.1.22"
  },
  "devDependencies": {
    "@types/node": "^17.0.44",
    "parcel": "^2.6.0",
    "typescript": "^4.7.2"
  }
}
