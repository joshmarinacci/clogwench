{
  "name": "musicplayer",
  "version": "0.0.1",
  "main": "dist/node.js",
  "type": "module",
  "dependencies": {
    "parcel": "^2.6.0",
    "typescript": "^4.7.2",
    "thneed-gfx": "0.1.34"
  },
  "devDependencies": {
    "@types/node": "^17.0.44"
  },
  "scripts": {
    "dev-node": "npm run clean && npx parcel build src/node.ts && node dist/node.js",
    "clean": "rm -rf dist .parcel-cache",
    "dev-browser": "npm run clean && npx parcel src/browser.html"
  }
}
